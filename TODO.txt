 * config headers?
 * clang-tidy // run from command line after -DEXPORT_COMPILE_COMMANDS
 * clang-check // ditto
 * clang sanitizers // -DCMAKE_CXX_FLAGS=-fsanitize={undefined,address,thread}
 * clang-format
 * travis // got compilers, just need sanitizers
 * catch unit tests
 * appveyor
 * git diff --check
 * valgrind
 * doxygen/standardese

MAYBE:
 * Codecov/Coveralls
 * Codacy/Coverity Scan
